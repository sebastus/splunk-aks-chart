apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "splunkchart.fullname" . }}-test-connection"
  labels:
    app.kubernetes.io/name: {{ include "splunkchart.name" . }}
    helm.sh/chart: {{ include "splunkchart.chart" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
    app.kubernetes.io/managed-by: {{ .Release.Service }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "splunkchart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
